# 2026-09-01 工作日志

## 生成 MGin 完整应用文档
- 任务：为 MGin 微服务框架（github.com/maczh/mgin，Go 1.25）生成一份完整应用文档。
- 调研方式：用 Explore 代理做全包公开 API 梳理，并直接核验关键源码（result.go、dao/mysql.go、client.go、db/db.go、redis.go、app.go、mgin.go、configure.go）。
- 产出：`docs/MGin应用文档.md`（1092 行，19 章），覆盖：框架概述、快速开始、应用生命周期、配置体系、8 类数据库/消息中间件（MySQL/PostgreSQL/SQLite/MongoDB/Redis/ClickHouse/ES/Kafka）、DAO 泛型层、缓存、全部中间件、微服务调用、i18n/errcode、Result、日志、注册中心、配置中心、内置系统模块+Casbin、工具集、最佳实践、FAQ、版本说明。
- 关键事实确认：全局 DB 变量名 db.Mysql/Pg/Sqlite/Mongo/Redis/ElasticSearch/Kafka/Clickhouse；Result 构造器 models.Success/Error/ErrorT/SuccessWithPage；client.Call/CallT；NewApp(configFile,appName,version,xlang)；MongoDB 需 ReturnConnection 归还 session。

## 后续可关注
- 现有 `sdk_document.md`（60KB）与新建文档有重叠，可考虑合并或互相引用。
- examples 目录（README 提到的 mgin-server/mgin-client）在仓库中不存在，文档中的示例为自写。

## 生成英文版文档
- 任务：在中文文档基础上生成英文版。
- 产出：`docs/MGin Application Documentation.md`（1093 行，19 章 + 目录，结构与中文版一一对应），代码与 API 引用保持一致，仅翻译正文与代码注释。

## 针对 jh 分支补充文档（中英文两份）
- 背景：当前仓库位于 `jh` 分支（寄海版，最新 tag v1.25.11-jh），相对 master-1.25 是精简变体。
- 已核实的 jh 分支关键差异：
  - 删除内置 `sys` 模块（controller/dao/service/route/middle 全套）与 Swagger 文档（docs/docs.go、swagger.json/yaml），目的缩小编译体积。
  - 新增 GORM 二级缓存层（go-gorm/caches/v4 + 自定义 RedisCacher）：MySQL/SQLite/PostgreSQL/ClickHouse 的 UseCache()，默认过期 5 分钟（MySQL=`mysql_cache_expired` 秒、Postgres=`postgres.cache.expired` 秒；ClickHouse 未设 Expiration→不过期）。
  - 新增 PostgreSQL 支持（db.Pg + PostgresDao[E]）、ClickHouse 支持（db.Clickhouse + ClickhouseDao[E]）。
  - NewApp 新增 `-v` 版本打印与 GetVersion()；GIN 模式改由 go.application.debug 控制；etcd 加 lease+keepalive；Nacos 改社区 nacos-sdk-go；CORS 支持外部配置；MongoDB 驱动换官方 mongo-driver。
- **已知 bug**：ClickhouseDao.MultiCreate 误用 db.Mysql.GetConnection，会把批量数据写进 MySQL（截至 v1.25.11-jh）。
- 改动：两份文档均新增「第 20 章 jh 分支专项说明」(差异总览/已移除/缓存/PG/CH/其他改进/已知问题/迁移建议)，并在第 15 章(sys)、4.4、5.1、5.2、5.6、8.1、13 处插入 jh 提示与修正（如 Postgres 缓存键由 `cache: true` 改为 `cache.enabled/expired`）。中文 1092→1231 行，英文 1093→1232 行。

## 补充 utils 文档 + 重排中英文文档结构
- 任务：按用户要求补充 `utils` 工具函数文档，并重新调整两份文档的组织结构。
- utils 调研：用 Explore 代理梳理 `utils/` 下全部 46 个 .go 文件（crypto/string/time/json/map-struct/slice/ip/file/uuid/concurrent/validate 等），核对所有导出函数签名。
- 结构重排：将两份文档由平铺 1–20 章，重排为 **7 大部分**（入门与架构 / 配置管理 / 数据访问 / 服务与通信 / 请求处理与横切 / 内置模块与工具 / 参考与分支），章节重新编号：配置中心 14→5、数据库 5→6、DAO 6→7、缓存 7→8、客户端 9 不变、注册中心 13→10、中间件 8→11、i18n 10→12、Result 11→13、日志 12→14、内置 15→15、utils 16→16、最佳实践/FAQ/版本/ jh(20) 不变。目录（TOC）与正文均按部分分组并重建锚点。
- utils 章节（第 16 章）由简要列表扩写为完整参考：12 个子节（16.1 加解密 ~ 16.12 其它），含函数签名表、示例与「实现备注」（LinkList.Get 越界 panic、ExpireCache 误用 value 作 key、sqlvalidate 黑名单首字符漏判、SftpConnect 跳过 HostKey、IsPortUse 语义相反等已知边界问题）。
- 实现方式：先用 Python 脚本（/tmp/reorg.py → fix.py → banner_fix.py → regen_toc.py）做章节重排/重新编号/目录重建/横幅插入。⚠️ 踩坑：`reorg.py` 非幂等，连跑两次会把编号应用两次导致错乱；后改用 REMAP_INV 的 fix.py 纠正，并用 regen_toc.py 单独重建目录。最终两份文档：20 章、14 个部分横幅（7 TOC + 7 正文）、utils 12 子节、jh 交叉引用链接保持有效。
- 产出行数：中文 `docs/MGin应用文档.md`、英文 `docs/MGin Application Documentation.md` 各约 1500+ 行。
